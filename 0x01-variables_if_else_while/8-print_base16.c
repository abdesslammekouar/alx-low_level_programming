#include<stdio.h>
/**
 * main - program that prints all the numbers of base 16 in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int num;
char letter;
for (int = 0, int < 10, num++)
putchar((num % 10) + '0');
for (char = a, char <= f, letter++)
putchar(letter);
putchar("\n");
return (0);
}
